document.querySelectorAll('.video-shell').forEach(shell=>{const video=shell.querySelector('video');const btn=shell.querySelector('.sound');shell.addEventListener('mouseenter',()=>{video.play().catch(()=>{})});shell.addEventListener('mouseleave',()=>{video.pause()});btn.addEventListener('click',()=>{video.muted=!video.muted;btn.textContent=video.muted?'Ton an':'Ton aus';if(video.paused)video.play().catch(()=>{})})});