const modal=document.querySelector('#projectModal');const video=document.querySelector('#modalVideo');const title=document.querySelector('#modalTitle');const meta=document.querySelector('#modalMeta');const close=document.querySelector('.close');document.querySelectorAll('.project-card').forEach(card=>{card.addEventListener('click',()=>{title.textContent=card.dataset.title||'';meta.textContent=card.dataset.meta||'';video.src=card.dataset.video||'';video.load();modal.showModal();document.body.classList.add('modal-open');const p=video.play();if(p&&p.catch)p.catch(()=>{});});});function closeModal(){video.pause();video.removeAttribute('src');video.load();if(modal.open)modal.close();document.body.classList.remove('modal-open');}close.addEventListener('click',closeModal);modal.addEventListener('click',e=>{if(e.target===modal)closeModal();});modal.addEventListener('close',()=>document.body.classList.remove('modal-open'));document.addEventListener('keydown',e=>{if(e.key==='Escape'&&modal.open)closeModal();});